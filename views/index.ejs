<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="icon" href="/logo.png">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* Hide number input scroll arrows in WebKit browsers */
    input[type=number]::-webkit-inner-spin-button,
    input[type=number]::-webkit-outer-spin-button {
      -webkit-appearance: none;
      margin: 0;
    }
  </style>
  <title>Snipzy</title>
</head>
<body class="bg-black text-lime-300 min-h-screen flex items-center justify-center">

  <div class="flex flex-col items-center justify-center w-full px-4">
  <img src="/sniplogo.png" alt="logo"/>

    <form action="/signup" method="POST"
      class="w-full max-w-lg flex flex-col gap-4 bg-black p-6 rounded-md border border-green-500">

      <input type="text" name="name" placeholder="Name" required
        class="bg-black border border-green-400 text-green-400 placeholder-white px-4 py-2 rounded outline-none" />

      
      <input type="email" name="email" placeholder="Email ID" required
        class="bg-black border border-green-400 text-green-400 placeholder-white px-4 py-2 rounded outline-none" />

      
      <input type="password" name="password" placeholder="Password" required
        class="bg-black border border-green-400 text-green-400 placeholder-white px-4 py-2 rounded outline-none" />

     
      <button type="submit"
        class="bg-green-500 text-black font-semibold py-2 rounded hover:bg-green-600 transition">
        Create
      </button>

      <!-- Login link -->
      <a href="/login"
        class="block w-full bg-green-500 text-black text-center font-semibold py-2 rounded hover:bg-green-600 transition">
        Login
      </a>

    </form>
  </div>

</body>
</html>
