<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="icon" href="/logo.png">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* Hide number input scroll arrows in WebKit browsers */
    input[type=number]::-webkit-inner-spin-button,
    input[type=number]::-webkit-outer-spin-button {
      -webkit-appearance: none;
      margin: 0;
    }
  </style>
  <title>Snipzy</title>
</head>
<body class="bg-black text-green-400">

  <!-- Full height container with flex centering -->
  <div class="min-h-screen flex items-center justify-center px-4">

    <!-- Form container with max-width and responsive width -->
    <form action="/signup" method="POST" class="w-full max-w-md flex flex-col gap-4 bg-black p-6 rounded-md border border-green-500">

      <!-- Name Input -->
      <input type="text" name="name" placeholder="Name" required
        class="bg-black border border-green-500 text-green-300 placeholder-white px-4 py-2 rounded outline-none" />

      <!-- Email Input -->
      <input type="email" name="email" placeholder="Email ID" required
        class="bg-black border border-green-500 text-green-300 placeholder-white px-4 py-2 rounded outline-none" />

      <!-- Password Input -->
      <input type="password" name="password" placeholder="Password" required
        class="bg-black border border-green-500 text-green-300 placeholder-white px-4 py-2 rounded outline-none" />

      <!-- Submit Button -->
      <button type="submit"
        class="bg-green-500 text-black font-semibold py-2 rounded hover:bg-green-600 transition">
        Create
      </button>

      <!-- Login Link Button -->
      <a href="/login"
        class="block w-full bg-green-500 text-black text-center font-semibold py-2 rounded hover:bg-green-600 transition">
        Login
      </a>

    </form>
  </div>

</body>
</html>